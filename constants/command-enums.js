// all commands are case insensitive
const COMMANDS = ['command', 'commands', 'help', 'list', 'listCommands'];
const JOKE = ['joke', 'jokes', 'jokez'];
const SERVER = ['server', 'servers'];
const USER_INFO = ['user-info', 'userInfo'];

module.exports = {
  COMMANDS,
  JOKE,
  SERVER,
  USER_INFO,
};
